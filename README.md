# 🤖 Valentine's Day Telegram Bot - Python (aiogram)

Телеграм бот на Python с использованием aiogram для управления День Святого Валентина Mini App.

## 📋 Требования

- Python 3.8+
- pip (встроен с Python)
- Railway аккаунт (для развертывания)
- Telegram бот токен (от BotFather)

## 🚀 Быстрый старт (Локально)

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/USERNAME/valentine-bot-python.git
cd valentine-bot-python
```

### 2. Создайте виртуальное окружение

```bash
# На Windows
python -m venv venv
venv\Scripts\activate

# На macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Создайте файл `.env`

```bash
cp .env.example .env
```

Откройте `.env` и вставьте ваш токен от BotFather:

```
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
MINI_APP_URL=https://username.github.io/valentine-site
```

### 5. Запустите бота

```bash
python bot.py
```

Бот будет работать и слушать сообщения!

## 🚀 Развертывание на Railway

Railway автоматически будет использовать `Procfile` и `requirements.txt`.

### Шаг 1: Подготовьте репозиторий

```bash
git init
git add .
git commit -m "Valentine bot on Python with aiogram"
git remote add origin https://github.com/USERNAME/valentine-bot-python.git
git branch -M main
git push -u origin main
```

### Шаг 2: Подключите Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите **New Project → GitHub Repo**
3. Выберите ваш репозиторий `valentine-bot-python`
4. Railway автоматически обнаружит `Procfile`

### Шаг 3: Установите переменные окружения

В Railway project settings добавьте переменные:

```
BOT_TOKEN = your_bot_token_here
MINI_APP_URL = https://username.github.io/valentine-site
```

### Шаг 4: Deploy

Нажмите **Deploy** - Railway автоматически найдет:
- `requirements.txt` для установки зависимостей
- `Procfile` для запуска `python bot.py`

Бот начнет работать автоматически! 🎉

## 📱 Использование

### Команды бота

- `/start` - Начать, получить кнопку Mini App
- `/help` - Справка по командам
- `/status` - Статус бота и информация о пользователе

### Функции

✨ **Основное**:
- Кнопка "💌 Открыть сюрприз" которая открывает Mini App
- Реагирует на ключевые слова (любовь, чувства)
- Красивые ответы и эмодзи

🎯 **Расширение**:
- Можно добавить сохранение статистики (сколько раз открыли)
- Отправка ежедневных напоминаний
- Рейтинг самых преданных пользователей

## 🔧 Кастомизация

### Изменить текст при /start

Отредактируйте функцию `cmd_start()`:

```python
welcome_text = (
    f"💕 Привет, {first_name}!\n\n"
    f"Твой кастомный текст здесь... 💘"
)
```

### Добавить новую команду

```python
@dp.message(Command("your_command"))
async def cmd_your_command(message: types.Message):
    """Описание команды"""
    text = "Ответ на команду"
    await message.answer(text)
```

### Добавить обработку реакций

```python
@dp.message(F.text.contains("ключевое слово"))
async def handler_name(message: types.Message):
    """Реагирует на ключевое слово"""
    await message.answer("Ответ")
```

### Добавить кнопки с параметрами

```python
keyboard = InlineKeyboardMarkup(
    inline_keyboard=[
        [
            InlineKeyboardButton(
                text="Текст кнопки",
                callback_data="callback_data"
            )
        ]
    ]
)
await message.answer("Текст", reply_markup=keyboard)
```

## 📊 Логирование

Все важные события логируются в консоль:
- Запуск бота
- Новые пользователи (команда /start)
- Ошибки
- URL Mini App

Проверяйте логи в Railway Dashboard!

## 🔐 Безопасность

- ✅ Не коммитьте `.env` файл!
- ✅ Используйте Railway Secrets для хранения токена
- ✅ Включена валидация входных данных
- ✅ Обработка ошибок включена

## 🆘 Troubleshooting

### "ModuleNotFoundError: No module named 'aiogram'"

```bash
pip install -r requirements.txt
```

### "Bot token is invalid"

- Проверьте токен в `.env`
- Убедитесь, что скопировали весь токен
- Создайте нового бота через @BotFather если забыли

### Railway не запускает бота

1. Проверьте логи в Railway Dashboard
2. Убедитесь, что файл `Procfile` существует
3. Убедитесь, что `requirements.txt` корректен
4. Проверьте переменные окружения (`BOT_TOKEN`)

### Bot не отвечает

- Проверьте интернет соединение
- Убедитесь, что токен правильный
- Перезагрузите бота в Railway

## 📚 Дополнительное чтение

- [Документация aiogram](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Railway документация](https://docs.railway.app/)
- [Python asyncio](https://docs.python.org/3/library/asyncio.html)

## 🎨 Код и стиль

- Используется PEP 8
- Асинхронный код (async/await)
- Логирование для отладки
- Обработчики ошибок

## 💡 Идеи расширений

- [ ] Сохранение статистики в БД
- [ ] Ежедневные напоминания
- [ ] Счетчик открытий Mini App
- [ ] Рейтинг пользователей
- [ ] Интеграция с платежами
- [ ] Отправка фото через бота

---

**Создано с ❤️ для самого важного человека** 💕

**Happy Valentine's Day!** 💌
